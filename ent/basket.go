// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
	"github.com/SeyramWood/ent/agent"
	"github.com/SeyramWood/ent/basket"
	"github.com/SeyramWood/ent/customer"
	"github.com/SeyramWood/ent/merchant"
	"github.com/SeyramWood/ent/product"
)

// Basket is the model entity for the Basket schema.
type Basket struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BasketQuery when eager-loading is set.
	Edges            BasketEdges `json:"edges"`
	agent_baskets    *int
	customer_baskets *int
	merchant_baskets *int
	product_baskets  *int
}

// BasketEdges holds the relations/edges for other nodes in the graph.
type BasketEdges struct {
	// Merchant holds the value of the merchant edge.
	Merchant *Merchant `json:"merchant,omitempty"`
	// Agent holds the value of the agent edge.
	Agent *Agent `json:"agent,omitempty"`
	// Customer holds the value of the customer edge.
	Customer *Customer `json:"customer,omitempty"`
	// Product holds the value of the product edge.
	Product *Product `json:"product,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [4]bool
}

// MerchantOrErr returns the Merchant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BasketEdges) MerchantOrErr() (*Merchant, error) {
	if e.loadedTypes[0] {
		if e.Merchant == nil {
			// The edge merchant was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: merchant.Label}
		}
		return e.Merchant, nil
	}
	return nil, &NotLoadedError{edge: "merchant"}
}

// AgentOrErr returns the Agent value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BasketEdges) AgentOrErr() (*Agent, error) {
	if e.loadedTypes[1] {
		if e.Agent == nil {
			// The edge agent was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: agent.Label}
		}
		return e.Agent, nil
	}
	return nil, &NotLoadedError{edge: "agent"}
}

// CustomerOrErr returns the Customer value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BasketEdges) CustomerOrErr() (*Customer, error) {
	if e.loadedTypes[2] {
		if e.Customer == nil {
			// The edge customer was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: customer.Label}
		}
		return e.Customer, nil
	}
	return nil, &NotLoadedError{edge: "customer"}
}

// ProductOrErr returns the Product value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BasketEdges) ProductOrErr() (*Product, error) {
	if e.loadedTypes[3] {
		if e.Product == nil {
			// The edge product was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: product.Label}
		}
		return e.Product, nil
	}
	return nil, &NotLoadedError{edge: "product"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Basket) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case basket.FieldID:
			values[i] = new(sql.NullInt64)
		case basket.FieldCreatedAt, basket.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case basket.ForeignKeys[0]: // agent_baskets
			values[i] = new(sql.NullInt64)
		case basket.ForeignKeys[1]: // customer_baskets
			values[i] = new(sql.NullInt64)
		case basket.ForeignKeys[2]: // merchant_baskets
			values[i] = new(sql.NullInt64)
		case basket.ForeignKeys[3]: // product_baskets
			values[i] = new(sql.NullInt64)
		default:
			return nil, fmt.Errorf("unexpected column %q for type Basket", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Basket fields.
func (b *Basket) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case basket.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			b.ID = int(value.Int64)
		case basket.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				b.CreatedAt = value.Time
			}
		case basket.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				b.UpdatedAt = value.Time
			}
		case basket.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field agent_baskets", value)
			} else if value.Valid {
				b.agent_baskets = new(int)
				*b.agent_baskets = int(value.Int64)
			}
		case basket.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field customer_baskets", value)
			} else if value.Valid {
				b.customer_baskets = new(int)
				*b.customer_baskets = int(value.Int64)
			}
		case basket.ForeignKeys[2]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field merchant_baskets", value)
			} else if value.Valid {
				b.merchant_baskets = new(int)
				*b.merchant_baskets = int(value.Int64)
			}
		case basket.ForeignKeys[3]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field product_baskets", value)
			} else if value.Valid {
				b.product_baskets = new(int)
				*b.product_baskets = int(value.Int64)
			}
		}
	}
	return nil
}

// QueryMerchant queries the "merchant" edge of the Basket entity.
func (b *Basket) QueryMerchant() *MerchantQuery {
	return (&BasketClient{config: b.config}).QueryMerchant(b)
}

// QueryAgent queries the "agent" edge of the Basket entity.
func (b *Basket) QueryAgent() *AgentQuery {
	return (&BasketClient{config: b.config}).QueryAgent(b)
}

// QueryCustomer queries the "customer" edge of the Basket entity.
func (b *Basket) QueryCustomer() *CustomerQuery {
	return (&BasketClient{config: b.config}).QueryCustomer(b)
}

// QueryProduct queries the "product" edge of the Basket entity.
func (b *Basket) QueryProduct() *ProductQuery {
	return (&BasketClient{config: b.config}).QueryProduct(b)
}

// Update returns a builder for updating this Basket.
// Note that you need to call Basket.Unwrap() before calling this method if this Basket
// was returned from a transaction, and the transaction was committed or rolled back.
func (b *Basket) Update() *BasketUpdateOne {
	return (&BasketClient{config: b.config}).UpdateOne(b)
}

// Unwrap unwraps the Basket entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (b *Basket) Unwrap() *Basket {
	tx, ok := b.config.driver.(*txDriver)
	if !ok {
		panic("ent: Basket is not a transactional entity")
	}
	b.config.driver = tx.drv
	return b
}

// String implements the fmt.Stringer.
func (b *Basket) String() string {
	var builder strings.Builder
	builder.WriteString("Basket(")
	builder.WriteString(fmt.Sprintf("id=%v", b.ID))
	builder.WriteString(", created_at=")
	builder.WriteString(b.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", updated_at=")
	builder.WriteString(b.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Baskets is a parsable slice of Basket.
type Baskets []*Basket

func (b Baskets) config(cfg config) {
	for _i := range b {
		b[_i].config = cfg
	}
}
