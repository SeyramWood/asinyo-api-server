// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/SeyramWood/ent/pricemodel"
)

// PriceModel is the model entity for the PriceModel schema.
type PriceModel struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Initials holds the value of the "initials" field.
	Initials string `json:"initials,omitempty"`
	// Formula holds the value of the "formula" field.
	Formula string `json:"formula,omitempty"`
	// AsinyoFormula holds the value of the "asinyo_formula" field.
	AsinyoFormula string `json:"asinyo_formula,omitempty"`
	selectValues  sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PriceModel) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case pricemodel.FieldID:
			values[i] = new(sql.NullInt64)
		case pricemodel.FieldName, pricemodel.FieldInitials, pricemodel.FieldFormula, pricemodel.FieldAsinyoFormula:
			values[i] = new(sql.NullString)
		case pricemodel.FieldCreatedAt, pricemodel.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PriceModel fields.
func (pm *PriceModel) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case pricemodel.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			pm.ID = int(value.Int64)
		case pricemodel.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				pm.CreatedAt = value.Time
			}
		case pricemodel.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				pm.UpdatedAt = value.Time
			}
		case pricemodel.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				pm.Name = value.String
			}
		case pricemodel.FieldInitials:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field initials", values[i])
			} else if value.Valid {
				pm.Initials = value.String
			}
		case pricemodel.FieldFormula:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field formula", values[i])
			} else if value.Valid {
				pm.Formula = value.String
			}
		case pricemodel.FieldAsinyoFormula:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field asinyo_formula", values[i])
			} else if value.Valid {
				pm.AsinyoFormula = value.String
			}
		default:
			pm.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PriceModel.
// This includes values selected through modifiers, order, etc.
func (pm *PriceModel) Value(name string) (ent.Value, error) {
	return pm.selectValues.Get(name)
}

// Update returns a builder for updating this PriceModel.
// Note that you need to call PriceModel.Unwrap() before calling this method if this PriceModel
// was returned from a transaction, and the transaction was committed or rolled back.
func (pm *PriceModel) Update() *PriceModelUpdateOne {
	return NewPriceModelClient(pm.config).UpdateOne(pm)
}

// Unwrap unwraps the PriceModel entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pm *PriceModel) Unwrap() *PriceModel {
	_tx, ok := pm.config.driver.(*txDriver)
	if !ok {
		panic("ent: PriceModel is not a transactional entity")
	}
	pm.config.driver = _tx.drv
	return pm
}

// String implements the fmt.Stringer.
func (pm *PriceModel) String() string {
	var builder strings.Builder
	builder.WriteString("PriceModel(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pm.ID))
	builder.WriteString("created_at=")
	builder.WriteString(pm.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(pm.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(pm.Name)
	builder.WriteString(", ")
	builder.WriteString("initials=")
	builder.WriteString(pm.Initials)
	builder.WriteString(", ")
	builder.WriteString("formula=")
	builder.WriteString(pm.Formula)
	builder.WriteString(", ")
	builder.WriteString("asinyo_formula=")
	builder.WriteString(pm.AsinyoFormula)
	builder.WriteByte(')')
	return builder.String()
}

// PriceModels is a parsable slice of PriceModel.
type PriceModels []*PriceModel
